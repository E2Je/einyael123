# primer.md - עין יעל · קורס AI
_עודכן: 19 ביולי 2026_

## Active Sprint
שיעור 2 (ChatGPT + NotebookLM + Canvas) בנוי במלואו בתוך `index.html`, כולל גלריית צילומי מסך מוסברת עם 10 תמונות ו-87 נקודות. ממתין לאישור/קומיט מהמשתמש.

## Completed This Session
- דף פתיחה (`#landing`) עם בחירה בין שיעור 1 לשיעור 2, מנגנון `setLesson(n)` + hash routing (`#lesson1`/`#lesson2`).
- שיעור 2 מלא: Hero, גלריית צילומים, מדריך ממשק ChatGPT+NotebookLM, השוואה ChatGPT מול Gemini, מקטע NotebookLM מפורט, מילון תוספות.
- המשתמש סיפק 10 צילומי מסך בתיקיית `צילומי מסך שיעור 2/` (2 ChatGPT, 3 Canvas, 5 NotebookLM). כל אחד נפתח ונותח בעיון, ונוספו להם 87 נקודות מוסברות בסך הכל (`shots2` array ב-index.html), עם התייחסות לכל אלמנט נראה במסך.
- שולבו עדכוני 2026 עדכניים לפי מדריכים רשמיים: Cinematic Video Overview, סגנונות אינפוגרפיקה מרובים, מעקב התקדמות בבוחן/כרטיסיות, יכולת NotebookLM לחפש מקורות בעצמו מהרשת (Deep Research בתוך המחברת), וחיבור Drive חי ל-ChatGPT.
- אומת ב-preview: כל 10 התמונות נטענות (naturalWidth ~1917px, תואם להנחת יחס 1920 רוחב שעליה מבוססים אחוזי המיקום של הנקודות), 87 נקודות רונדרו, קליק על נקודה פותח פופאפ עם התוכן הנכון, אין שגיאות קונסולה.
- כלי ה-screenshot של הדפדפן נתקע (timeout) בסביבה הזו - לא הצלחתי לצלם תצוגה חזותית ישירה, אך אימות ברמת ה-DOM (מיקומי נקודות בטווח 0-100%, תוכן פופאפ, טעינת תמונות) בוצע במלואו.

## Next Step
1. לשאול את המשתמש אם רוצה עוד 2-3 צילומי מסך שחסרים לכיסוי מלא יותר: (א) תשובת ChatGPT עם סרגל הכפתורים בפועל (הצג מקורות/שלבי חשיבה/ייצוא), (ב) פלייר Audio Overview בפועל מ-NotebookLM, (ג) תוצר בפועל מ-Canvas (למשל אינפוגרפיקה/בוחן שנוצרו).
2. לשאול שוב לגבי מקטע "משימות תרגול" לשיעור 2 - עדיין לא נבנה.
3. Commit + push - טרם בוצע, ממתין לאישור מפורש.

## Open Blockers
- אין חסימה טכנית.

## Key Decisions Made
- מיקום הנקודות בצילומי המסך חושב באחוזים לפי ניתוח חזותי מדוקדק של כל תמונה (לא מדידה בפיקסלים מדויקת) - אותה שיטה כמו בשיעור 1.
- כל אלמנט נראה במסך קיבל נקודה, כולל פיצ'רים פחות מרכזיים (GitHub, OpenAI Platform) עם הערה שהם רלוונטיים בעיקר למפתחים.
- עובדות עדכניות שולבו בטבעיות בתוך תיאורי הנקודות, לא בתיבת "עדכון" נפרדת.
